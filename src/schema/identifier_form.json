{
  "description": "An issued identifier",
  "id": "http://www.popoloproject.com/schemas/identifier.json#",
  "properties": {
    "identifier": {
      "attrs": {
        "placeholder": "An issued identifier, e.g. a DUNS number",
        "title": "An issued identifier, e.g. a DUNS number"
      },
      "title": "An issued identifier, e.g. a DUNS number",
      "type": "string"
    },
    "scheme": {
      "attrs": {
        "placeholder": "An identifier scheme, e.g. DUNS",
        "title": "An identifier scheme, e.g. DUNS"
      },
      "title": "An identifier scheme, e.g. DUNS",
      "type": "string"
    }
  },
  "required": [
  ],
  "title": "Identifier",
  "type": "object"
}
